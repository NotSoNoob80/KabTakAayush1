/* ============================================================
   DEPRECATED — replaced by mosaic.js
   ------------------------------------------------------------
   The drag/throw/zero-gravity physics gallery has been retired
   in favour of a calmer editorial mosaic grid. project.html now
   loads mosaic.js instead. This file is kept only because the
   workspace won't allow deleting it — it does nothing.
   ============================================================ */
